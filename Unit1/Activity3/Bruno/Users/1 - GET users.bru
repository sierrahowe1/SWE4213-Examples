meta {
  name: 1 - GET users
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/users/
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body[0].name: eq Ada Lovelace
}

settings {
  encodeUrl: true
  timeout: 0
}
