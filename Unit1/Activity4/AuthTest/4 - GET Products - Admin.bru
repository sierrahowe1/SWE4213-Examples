meta {
  name: 4 - GET Products - Admin
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/products?res.status=200
  body: none
  auth: inherit
}

params:query {
  res.status: 200
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.body.length: eq 4
}

settings {
  encodeUrl: true
  timeout: 0
}
