meta {
  name: 3 - LOGIN - Correct password
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "ada@example.com", "password": "12345678"
  }
}

tests {
  const body = res.getBody();         
  bru.setEnvVar("token", body.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
