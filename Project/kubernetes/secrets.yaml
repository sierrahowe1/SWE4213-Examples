# WARNING: This file contains sensitive credentials and must NEVER be committed to git.
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials  # Name used to reference this Secret in postgres.yaml
type: Opaque            # Generic key/value secret
data:
  # Values must be base64 encoded: echo -n "user" | base64
  POSTGRES_USER: dXNlcg==
  POSTGRES_PASSWORD: cGFzc3dvcmQ=