meta {
  name: 2 - LOGIN - Incorrect password
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "ada@example.com", "password": "incorrect"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
